server {
    listen ${PORT};
    root /usr/share/nginx/html;
    index index.html;

    # SPA: redirigir todo a index.html excepto archivos estáticos
    location / {
        try_files $uri $uri/ /index.html;
    }
}
